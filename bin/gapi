#!/bin/bash
cat <<'EOF'
Github cloud
curl -XGET -H 'Authorization: Bearer <personal token>' '<https://api.github.com/repos/><owner>/<repo>'

Github onprem
curl -XGET -H 'Authorization: Bearer <personal token>' 'https://<GHE url>/api/v3/repos/<owner>/<repo>'
EOF

#!/bin/bash
source "$(dirname "$0")/config.sh"

gcloud compute instances create "$VM_NAME" \
    --project="$PROJECT" \
    --zone="$ZONE" \
    --machine-type="$MACHINE_TYPE" \
    --image-family="$IMAGE_FAMILY" \
    --image-project="$IMAGE_PROJECT" \
    --tags="$TAGS"

# gcloud compute firewall-rules create allow-netcat-3333 \
#    --direction=INGRESS \
#    --priority=1000 \
#    --network=default \
#    --action=ALLOW \
#    --rules=tcp:3333 \
#    --source-ranges=0.0.0.0/0 \
#    --project=myfree-501315

# gcloud compute firewall-rules delete allow-netcat-3333 \
#    --project=myfree-501315 --quiet

